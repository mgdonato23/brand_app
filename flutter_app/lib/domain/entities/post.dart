// Classe que representa um post na aplicação
class Post {
  final int id; // Identificador único do post
  final String title; // Título do post
  final String body; // Corpo do post (conteúdo)

  // Construtor da classe Post
  Post({required this.id, required this.title, required this.body});
}
